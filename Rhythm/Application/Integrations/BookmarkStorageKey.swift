import Foundation

enum BookmarkStorageKey {
    static let claudeDesktopConfig = "bookmark.claudeDesktopConfig"
    static let openClawConfig = "bookmark.openClawConfig"
    static let thingsDatabase = "bookmark.thingsDatabase"
}
